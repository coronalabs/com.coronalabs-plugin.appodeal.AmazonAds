Sources for the plugin `plugin.appodeal.AmazonAds`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.AmazonAds" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
